# 無料枠モードで使う開発者のGemini APIキー（サーバー専用。NEXT_PUBLIC_ を付けないこと）
GEMINI_API_KEY=

# サーバー側レート制限（Upstash Redis）。未設定ならインメモリにフォールバック。
# Vercel Marketplace連携では KV_REST_API_URL / KV_REST_API_TOKEN が自動注入される（コードは両対応）
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
